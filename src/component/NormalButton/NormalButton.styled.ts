import styled from 'styled-components';

const NormalButtonWrapper = styled.button`
  background: white;
  color: black;
  border: 0.5px solid rgba(0, 0, 0, 0.3);
`;

export default { NormalButtonWrapper };
